// Les Méthodes (Les Actions)
// Un objet ne sert pas qu'à stocker des données (Attributs), il sert aussi à faire des choses (Méthodes).

// Attributs = Ce que l'objet EST (Adjectifs/Noms : titre, couleur...).

// Méthodes = Ce que l'objet FAIT (Verbes : afficher, calculer, démarrer...).

// La Syntaxe d'une Méthode
// Nous allons déplacer l'affichage à l'intérieur de la classe Livre.

// public void nomDeLAction() {
//     // Le code de l'action
// }

// void : Signifie que l'action ne renvoie pas de réponse (comme un calcul), elle fait juste quelque chose.